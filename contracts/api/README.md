# API Contracts

Start from a small set of stable contracts:

- `GET /v1/bootstrap`
- auth endpoints
- onboarding step endpoints
- discovery feed endpoints
- swipe and match endpoints
- compatibility endpoints
- chat bootstrap endpoints

The mobile app should rely on these contracts instead of inferring backend behavior from UI needs.
