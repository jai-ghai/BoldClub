# Infrastructure

This folder is reserved for operational setup.

## Planned Areas

- `db` for migrations, seed data, and local schema bootstrapping
- `docker` for local development containers
- `storage` for media handling notes and S3-related setup

Keep infrastructure artifacts here instead of scattering them across app folders.
